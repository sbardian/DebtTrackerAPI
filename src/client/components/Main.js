import React from 'react';
import PropTypes from 'prop-types';

const Main = props => (
  <div className="main-container">
    {props.children}
  </div>
);

Main.propTypes = {

};

export default Main;
